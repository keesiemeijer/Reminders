import { ListSettings } from "./types";

// Defaults used for a list
export const SettingDefault: ListSettings = {
    type: "",
    title: "",
    description: "",
    orderByDate: true,
    settings: {},
};
